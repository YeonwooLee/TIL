```sql
-- 인자 대체, 4번째 속성을 4로
select
	name, PParea, popuLong,
    round(PParea/popuLong,3) '1인당 점유면적'
    from country
    where popuLong >= 100000000
    order by 4 asc -- 4 == 4번째 속성 == round(PParea/popuLong,3)
    ;

```